# Function summary
>### read(url: string): Promise<FileContent>
>Read the given file based on the file relative url and return a Promise\<FileContent>.
>The function only can read html and json files. Not working with other file extensions.